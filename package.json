{
  "name": "sentinel",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "generate:torn-types": "pnpm --filter @sentinel/shared generate-types && pnpm --filter @sentinel/shared build",
    "supabase:push": "pnpm dlx supabase db push --local && pnpm dlx supabase db push"
  },
  "devDependencies": {
    "openapi-typescript": "^7.10.1"
  }
}
